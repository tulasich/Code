//delete an element in an array
#include<stdio.h>
int main(){
    int n, i;
    printf("Enter array size: ");
    scanf("%d",&n);
    int a[n];
    printf("Enter array elements:\n");
    for(i=0; i<n; i++){
        scanf("%d", &a[i]);
    }
    int index;
    printf("Index at which element to be deleted: ");
    scanf("%d", &index);

    

}